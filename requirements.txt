# Modern AI Systems — Week 1
#
# Pinned so the whole class is on one version, for the same reason the Neo4j
# image tag is pinned: when we debug together, we debug the same thing.
#
# Week 1 needs only the first two. The Week 3 block below is already listed so
# the class installs one pinned set rather than drifting apart mid-semester;
# it is small and has no model download. Weeks 5+ add the neo4j driver and
# deepeval, which are not here yet.

google-genai==2.22.0
python-dotenv==1.2.3

# Week 3 — chunking, semantic search, and a persistent vector store on disk.
# Embeddings come from the API (see embedding_client.py), so there is no model
# download here and no torch.
chromadb==1.5.9

# Optional, not installed by default. Only the built-in demo in multimodal_demo.py
# draws its own images and needs this; that script works on your own image files
# without it. Uncomment if you want the zero-argument demo. Verified with 10.0.1.
# Pillow==10.0.1

# Runs prompt_lab.ipynb inside VS Code. Not full JupyterLab -- VS Code has the
# notebook UI built in, so this is only the kernel.
#
# On Windows: install from a SHORT path (C:\dev\modern-ai, not a deep folder
# inside OneDrive Documents). One of ipykernel's dependencies ships paths that
# exceed the 260-character limit, and the resulting OSError is unhelpful.
ipykernel==7.3.0
